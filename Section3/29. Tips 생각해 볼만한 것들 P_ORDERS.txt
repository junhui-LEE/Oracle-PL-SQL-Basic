create or replace NONEDITIONABLE PACKAGE P_ORDERS AS 

  /* TODO enter package declarations (types, exceptions, methods etc) here */ 
  V_P_ORDERS VARCHAR2(500) := 'P_ORDERS 패키지는 주문과 관련된 패키지 입니다..';
 
    /* 포인트 가져오는 펑션 */
    FUNCTION PF_GET_ADDPOINT
    (
        P_PRICE IN NUMBER
    )RETURN NUMBER
    ;
    
    /* 미성년자는 맥주 안됨 */
    FUNCTION PF_GET_NOBEER(
        P_CST_ID IN VARCHAR2,  -- 고객 ID
        P_MNU_ID IN VARCHAR2   -- 메뉴 ID
    )RETURN VARCHAR2;
    
    /* 주문서 만드는 프로시저 */
    PROCEDURE PSP_MAKE_ORDER
    (
          R_RTN_CODE OUT VARCHAR2 
        , R_RTN_MSG OUT VARCHAR2 
        , P_CST_ID IN VARCHAR2 
        , P_MNU_ID IN VARCHAR2 DEFAULT NULL
    )
    ;

END P_ORDERS;