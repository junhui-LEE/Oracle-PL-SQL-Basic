DECLARE 
    R_RTN_CODE VARCHAR2(500);
    R_RTN_MSG VARCHAR2(500);
BEGIN
    P_ORDERS.PSP_MAKE_ORDER(NULL, R_RTN_CODE, R_RTN_MSG);
    DBMS_OUTPUT.PUT_LINE('R_RTN_CODE -> ' || R_RTN_CODE);
    DBMS_OUTPUT.PUT_LINE('R_RTN_MSG -> ' || R_RTN_MSG);
    IF R_RTN_CODE = -20055 THEN
        COMMIT;
        DBMS_OUTPUT.PUT_LINE('커밋');
    ELSE
        ROLLBACK;
    END IF;
    
END;









